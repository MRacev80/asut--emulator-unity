# Бизнес-процесс: от задачи до готового ПО

> Полный цикл: от брифа заказчика → до сдачи проверенного кода с документацией.

---

## Обзорная схема (уровень 1)

```
ВХОД                    ПРОЦЕСС                         ВЫХОД
─────────────────────────────────────────────────────────────────
Задача заказчика   →    Spec.md                    →   ТЗ на FB
Spec.md            →    Claude + MCP + CODESYS     →   ST-код
ST-код             →    Тесты уровней 1–4          →   Проверенный FB
Проверенный FB     →    Демо + MasterSCADA          →   Сданный объект
```

---

## Детальная диаграмма потока

```mermaid
flowchart TD
    START([🚀 Старт: задача от заказчика])
    START --> BRIEF

    subgraph BRIEF["📋 Этап 1 — Бриф и анализ"]
        B1["Описание объекта\nЧто управляем?\nКакое оборудование?"]
        B2["Определить сигналы\nВходы / выходы ПЛК\nАналог / дискрет"]
        B3["Определить логику\nУсловия пуска\nУсловия останова\nАварийные ситуации"]
        B1 --> B2 --> B3
    end

    BRIEF --> SPEC

    subgraph SPEC["📄 Этап 2 — Спецификация (spec.md)"]
        S1["Таблица тегов\nИмя / тип / OPC UA путь\nОписание / диапазон"]
        S2["Логика в plain text\nУсловие → Действие\nПриоритеты"]
        S3["Диаграмма состояний\nMermaid stateDiagram\nВсе переходы + аварии"]
        S4["Аварийные ситуации\nУсловие / Реакция / Приоритет"]
        S1 --> S2 --> S3 --> S4
    end

    SPEC --> REVIEW{Ревью spec.md\nвопросов нет?}
    REVIEW -- "❌ Вопросы есть" --> SPEC
    REVIEW -- "✅ Ок" --> GENERATE

    subgraph GENERATE["⚙️ Этап 3 — Генерация ST-кода"]
        G1["Claude читает spec.md\n+ шаблоны предыдущих FB"]
        G2["Claude пишет ST через MCP\ncreate_pou + set_pou_code"]
        G3["CODESYS компилирует\ncompile_project"]
        G4{Компиляция\nуспешна?}
        G1 --> G2 --> G3 --> G4
        G4 -- "❌ Ошибки" --> G2
    end

    G4 -- "✅ Скомпилировано" --> DEPLOY

    subgraph DEPLOY["🚀 Этап 4 — Деплой в эмулятор"]
        D1["download_to_plc\nsimulationMode=False"]
        D2["Control Win V3\nвыполняет ST-цикл ~20мс"]
        D3["OPC UA Server\nоткрыт на порту 4840"]
        D4["Symbol Configuration\nтеги опубликованы"]
        D1 --> D2 --> D3 --> D4
    end

    DEPLOY --> TEST1

    subgraph TEST1["🔬 Тест Уровень 1 — ST Unit"]
        T1A["Вход: MCP monitor_variable\nили CODESYS Watch"]
        T1B["Выставить входы вручную\nbEnable=TRUE, bReset=TRUE"]
        T1C["Проверить выходы\nnCount растёт / сбрасывается"]
        T1D{Логика\nверна?}
        T1A --> T1B --> T1C --> T1D
    end

    TEST1 --> T1D
    T1D -- "❌ Баг в ST" --> GENERATE
    T1D -- "✅ Логика верна" --> TEST2

    subgraph TEST2["🤖 Тест Уровень 2 — FAT Автоматизация"]
        T2A["Python Bridge запущен\npython bridge.py"]
        T2B["pytest читает теги через OPC UA\nconftest.py: connect/disconnect"]
        T2C["Тест 1: Пуск — счётчик растёт\nТест 2: Сброс — счётчик = 0\nТест 3: Порог — bDone = TRUE"]
        T2D["JSON/MD отчёт\nрезультат каждого теста"]
        T2E{Все тесты\nзелёные?}
        T2A --> T2B --> T2C --> T2D --> T2E
    end

    T2E -- "❌ Тест упал" --> GENERATE
    T2E -- "✅ Все зелёные" --> TEST3

    subgraph TEST3["🎮 Тест Уровень 3 — Mock (Unity без CODESYS)"]
        T3A["MockPlcSource включён\nUSE_MOCK=true"]
        T3B["Unity получает\nсинтетические данные"]
        T3C["Проверить визуализацию:\nцвета, индикаторы, анимации"]
        T3D["Проверить кнопки:\nResetButton, EnableButton"]
        T3E{Визуализация\nработает?}
        T3A --> T3B --> T3C --> T3D --> T3E
    end

    T3E -- "❌ Баг в Unity" --> UNITY_FIX["Правки в Unity C#-коде"]
    UNITY_FIX --> TEST3
    T3E -- "✅ Визуализация ок" --> TEST4

    subgraph TEST4["🔗 Тест Уровень 4 — Integration (end-to-end)"]
        T4A["CODESYS + Bridge + Unity\nвсе три запущены"]
        T4B["Bridge логирует timestamps\nCODESYS → Bridge → Unity"]
        T4C["Замерить latency\nцель < 100мс"]
        T4D["Проверить reconnect:\nперезапустить Bridge, ПЛК"]
        T4E{Latency < 100мс\nreconnect работает?}
        T4A --> T4B --> T4C --> T4D --> T4E
    end

    T4E -- "❌ Медленно / нестабильно" --> OPTIMIZE["Оптимизация Bridge\nили архитектуры"]
    OPTIMIZE --> TEST4
    T4E -- "✅ Стабильно" --> DEMO

    subgraph DEMO["🎥 Этап 5 — Демо заказчику"]
        DM1["Unity HMI — визуализация\nв реальном времени"]
        DM2["Показать нормальную работу\nзапуск, счётчик, светофор"]
        DM3["Показать аварию и сброс\nкнопки Reset / Стоп-Пуск"]
        DM4["Вопросы заказчика\nПравки по замечаниям"]
        DM1 --> DM2 --> DM3 --> DM4
    end

    DEMO --> SCADA

    subgraph SCADA["📊 Тест Уровень 5 — Acceptance (MasterSCADA 4)"]
        SC1["MasterSCADA подключается\nOPC UA → opc.tcp://localhost:4840"]
        SC2["Чеклист: все теги объекта\nвидны с корректными типами"]
        SC3["Проверить управление\nзапись тегов из SCADA"]
        SC4{Все теги ок\nуправление работает?}
        SC1 --> SC2 --> SC3 --> SC4
    end

    SC4 -- "❌ Теги не видны" --> SYMCONFIG["Добавить в\nSymbol Configuration"]
    SYMCONFIG --> SCADA
    SC4 -- "✅ Принято" --> DOCS

    subgraph DOCS["📚 Этап 6 — Документация"]
        DC1["Claude генерирует из spec.md\n+ ST-кода"]
        DC2["Таблица сигналов\nsignals.md"]
        DC3["FAT-отчёт\nJSON → MD-таблица"]
        DC4["Обновить TASKS.md\n+ ПРОЕКТ.md"]
        DC1 --> DC2 --> DC3 --> DC4
    end

    DOCS --> DONE([✅ Готово: сдан объект])
```

---

## Что на входе и выходе каждого этапа

| Этап | Вход | Инструмент | Выход |
|------|------|-----------|-------|
| **1. Бриф** | Словесное описание задачи | Разговор с заказчиком | Список сигналов, логика в словах |
| **2. Spec** | Список сигналов + логика | Текстовый редактор / Claude | `spec.md`: теги, stateDiagram, аварии |
| **3. Генерация ST** | `spec.md` | Claude + MCP + CODESYS | Скомпилированный FB без ошибок |
| **4. Деплой** | `.project` файл | MCP `download_to_plc` | Control Win V3 работает, OPC UA открыт |
| **Тест Ур.1** | Работающий ПЛК | MCP `monitor_variable` / Watch | Подтверждено: логика верна вручную |
| **Тест Ур.2** | OPC UA Server | Python pytest + asyncua | JSON/MD отчёт: N тестов / N прошли |
| **Тест Ур.3** | `MockPlcSource` | Unity Play mode | Подтверждено: визуализация без CODESYS |
| **Тест Ур.4** | Весь стек вместе | Bridge logs + замер latency | Latency < 100мс, reconnect стабилен |
| **5. Демо** | Unity HMI + стек | Live демо | Замечания заказчика, согласование |
| **Тест Ур.5** | MasterSCADA 4 + OPC UA | Чеклист тегов | Подписан акт приёмки FAT |
| **6. Документация** | spec.md + ST-код + отчёты | Claude | signals.md, FAT-отчёт, обновлённый ПРОЕКТ.md |

---

## Критические точки принятия решений

```
┌─────────────────────┬──────────────────┬───────────────────────────┐
│ Точка               │ Критерий «ОК»    │ Если НЕ ОК — куда идём   │
├─────────────────────┼──────────────────┼───────────────────────────┤
│ Ревью spec.md       │ Нет открытых     │ Уточнить у заказчика /    │
│                     │ вопросов         │ доработать spec           │
├─────────────────────┼──────────────────┼───────────────────────────┤
│ Компиляция ST       │ 0 ошибок         │ Claude правит ST-код      │
├─────────────────────┼──────────────────┼───────────────────────────┤
│ Тест Ур.1 (ST Unit) │ Логика верна     │ Правки в ST через MCP     │
│                     │ вручную          │                           │
├─────────────────────┼──────────────────┼───────────────────────────┤
│ Тест Ур.2 (FAT)     │ 100% тестов      │ Найти баг в ST или        │
│                     │ зелёные          │ в тест-кейсе              │
├─────────────────────┼──────────────────┼───────────────────────────┤
│ Тест Ур.4 (Integration)│ Latency<100мс │ Оптимизировать Bridge     │
│                     │ reconnect ок     │ или polling-интервал      │
├─────────────────────┼──────────────────┼───────────────────────────┤
│ Тест Ур.5 (Acceptance)│ Все теги в     │ Добавить в Symbol Config  │
│                     │ MasterSCADA      │ → перекомпилировать       │
└─────────────────────┴──────────────────┴───────────────────────────┘
```

---

## Время на каждый этап (ориентир)

| Этап | Первый FB | Повторный FB |
|------|-----------|-------------|
| Бриф + анализ | 2–4 ч | 1–2 ч |
| Spec.md | 2–3 ч | 30–60 мин |
| Генерация ST | 15–30 мин | 10–15 мин |
| Тест Ур.1 | 30–60 мин | 15–30 мин |
| Тест Ур.2 (FAT) | 2–4 ч (написать тесты) | 10 мин (запустить) |
| Тест Ур.3 (Mock) | 1–2 ч | 30 мин |
| Тест Ур.4 (Integration) | 1 ч | 15 мин |
| Демо | 1–2 ч | 30–60 мин |
| Тест Ур.5 (Acceptance) | 2–4 ч | 1–2 ч |
| Документация | 30–60 мин | 15–30 мин |
| **Итого** | **~15–25 ч** | **~4–8 ч** |

> **Ключевой момент:** первый FB на объекте — долго, зато шаблон написан. Каждый следующий FB — в 3–5 раз быстрее.

---

## Схема потоков данных (что куда идёт)

```
Заказчик                 Инженер                  Инструменты
────────                 ────────                 ────────────

Бриф        ──────────→  spec.md         ─────→   Claude (читает)
                                                      │
                                                      ↓ MCP
                                              CODESYS (ST-код)
                                                      │
                                                      ↓ OPC UA
                         bridge.py        ←─────  Control Win V3
                              │
                    ┌─────────┼─────────┐
                    ↓         ↓         ↓
                 Unity HMI  pytest   logs/
                 (демо)     (FAT)    (отчёт)
                    │
                    ↓
Заказчик  ←── демо на ноутбуке
                    │
                    ↓
MasterSCADA ←── OPC UA напрямую (Acceptance)
```
